# ══════════════════════════════════════════════════════════════════
# 白名單制（2026-09-20 改）：預設全部不托管，只有下列公開資產例外
# ══════════════════════════════════════════════════════════════════
# 為什麼從黑名單改成白名單：
#   黑名單漏列一個檔，它會自動被追蹤並公開——而且失敗是安靜的，
#   等到發現時東西已經在網路上了。（2026-08-14 踩過：檔案一改名，
#   按檔名寫的規則當場失效，差一步就被推上來。）
#   白名單相反：漏列只會「沒被托管」，看得見也修得掉。
#
#   附帶好處：黑名單必須寫出內部檔案的名字，那些名字本身就是
#   內部訊息（這個檔是公開可讀的）。白名單不需要提到它們。
#
# ⚠️ 新增公開資產時，要在下面補一條 ! 規則，否則不會被追蹤。
# ⚠️ 目錄要先 ! 解除忽略，才進得去裡面的檔案。

# ── 1) 預設：全部忽略 ──
*

# ── 2) 例外：根目錄的公開資產 ──
!.gitignore
!.nojekyll
!LICENSE
!README.md
!index.html
!yu-pdp.css

# ── 3) 例外：教學頁 ──
!product-page/
product-page/*
!product-page/product-page-guide.html
!product-page/product-page-guide.css
!product-page/product-page-guide.js

# ── 4) 例外：Skill 下載包與本體 ──
!product-page/londonimg-product.zip
!product-page/londonimg-product/
product-page/londonimg-product/*
!product-page/londonimg-product/README.md
!product-page/londonimg-product/SKILL.md
!product-page/londonimg-product/reference/
product-page/londonimg-product/reference/*
!product-page/londonimg-product/reference/block-html-templates.md
!product-page/londonimg-product/reference/page-blocks.md
!product-page/londonimg-product/reference/seo-geo-rules.md
!product-page/londonimg-product/reference/shopline-output.md
# 註：reference/ 底下只開上面四個檔。內部策略文件不在其中，維持不托管。
